*.DS_Store
*.pyc
*.egg-info
.idea/

xvfb.log
bin

trained_models
dist
requirements.resolved

# Local dev / generated (fork maintenance)
.venv/
venv/
docs/_build/
.pytest_cache/
recordings/*.mp4

# Procedural mining pack (regenerate with scripts/generate_mining_assets.py)
assets/mining_generated/*.png
assets/mining_generated/*.obj
assets/mining_generated/*.mtl

# RL training artefacts. Checkpoints + replay buffers are large (and go to
# the SSD by default — see scripts/rl/common.py). wandb/ is the local sync
# cache for the W&B client; the source of truth is the cloud.
wandb/
checkpoints/
runs/
